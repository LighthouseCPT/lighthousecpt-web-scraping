version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - echo "******** Running SAM Build ********"
      - sam build

artifacts:
  files:
    - template.yaml
  discard-paths: yes
